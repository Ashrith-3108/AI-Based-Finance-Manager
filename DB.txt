create database finance;
use finance;

create table register(username varchar(50), password varchar(150),
contact_no varchar(12),
email_id varchar(50),
address varchar(50));

create table feedback(username varchar(50), feedback varchar(500), feedback_date varchar(30), sentiment varchar(30));


